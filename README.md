# Countdown Timer ⏱️

A simple countdown timer built with pure JavaScript (ES Modules).

This project was created for practice with:
- working with dates (`Date`)
- modular JavaScript (`import / export`)
- DOM manipulation
- recursive timer updates


